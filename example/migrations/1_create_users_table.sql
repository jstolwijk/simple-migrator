CREATE TABLE application_user (
                                  id BIGINT NOT NULL PRIMARY KEY,
                                  name TEXT NOT NULL,
                                  email TEXT NOT NULL
);
